apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  namespace: obser
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush         1
        Daemon        Off
        Log_Level     info
        Parsers_File  parsers.conf

    [INPUT]
        Name              tail
        Path              /var/log/containers/*.log
        Parser            docker
        Tag               kube.*

    [FILTER]
        Name                kubernetes
        Match               kube.*

    [OUTPUT]
        Name  es
        Match *
        Host  elasticsearch.obser.svc.cluster.local
        Port  9200
        Index fluentbit

  parsers.conf: |
    [PARSER]
        Name        docker
        Format      json
